#Write a Python program to create an array with the integer values and determine the size of the memory occupied by the array. Comparison with list also.
import numpy as np
import sys
a=[1,2,3,4]
b=sys.getsizeof(a)
ar=np.array(a)
c=ar.size * ar.itemsize
print(b,c)
